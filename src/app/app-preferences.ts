import {environment} from "../environments/environment";

export let preferences = {
  theme: {
    color: environment.defaultPreferences.theme.color,
    mode: environment.defaultPreferences.theme.mode
  }
}
